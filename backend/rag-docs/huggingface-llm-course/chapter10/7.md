---
title: "End-of-chapter quiz"
chapter: "chapter10"
original_path: "chapters/en/chapter10/7.mdx"
source_revision: "5805d51523d561a82520b301dbc8c5759b212844"
source_url: "https://github.com/huggingface/course/blob/5805d51523d561a82520b301dbc8c5759b212844/chapters/en/chapter10/7.mdx"
---
<!-- DISABLE-FRONTMATTER-SECTIONS -->

# End-of-chapter quiz


Let's test what you learned in this chapter!

### 1. What can you use Argilla for?

**Choices**
- Turn unstructured data into structured data for NLP tasks
  - Explanation: You can use Argilla to add annotations to a dataset and use it for NLP tasks.
- Scrap a public website to build a dataset
  - Explanation: This is not a feature in Argilla, but you can scrap a public website and turn it into an Argilla dataset for annotations using the Python SDK.
- Improve the quality of an existing dataset
  - Explanation: You can use previous annotations as suggestions and correct them to improve the quality of a dataset.
- Adapt an existing dataset to your own use case
  - Explanation: You can use different question types in Argilla to adapt an existing dataset to your own use case.
- Train your model
  - Explanation: You cannot train a model directly in Argilla, but you can use the data you curate in Argilla to train your own model
- Generate synthetic datasets
  - Explanation: To generate synthetic datasets, you can use the distilabel package and then use Argilla to review and curate the generated data.

### 2. Argilla ONLY works in the Hugging Face Spaces and with Hugging Face Datasets.

**Choices**
- True
  - Explanation: You can also deploy Argilla locally using Docker and you can use the Python SDK to upload any type of data, including Hugging Face Datasets.
- False
  - Explanation: You can also deploy Argilla locally using Docker and you can use the Python SDK to upload any type of data, including Hugging Face Datasets.

### 3. You need a Hugging Face token to connect the Python SDK to your Argilla server.

**Choices**
- True
  - Explanation: This is only needed if your Argilla Space is private!
- False
  - Explanation: You don't need a token if you are using a public Argilla Space or a local deployment with Docker.

### 4. What are **fields** in Argilla? How many fields can you use?

**Choices**
- Fields show the data that we are annotating. All this information needs to be collected in a single field.
  - Explanation: You can spread the information across multiple fields, if you need to.
- Fields show the data that we are annotating. All this information can be spread across multiple fields.
  - Explanation: Yes, you can have multiple fields and also fields of different types (text, chat, image...) depending on the type of data you're annotating.
- Fields contain the metadata of the records. You can use as many as you need.
  - Explanation: You can have both fields and metadata in your dataset, but they serve separate purposes. Metadata are used for filtering and sorting purposes as extra information, while fields show the data that we are annotating.

### 5. What's the best type of question for a token classification task?

**Choices**
- A SpanQuestion
  - Explanation: SpanQuestions let you highlight bits of text and apply a label to them. This is the best type for a token classification task.
- A LabelQuestion
  - Explanation: This type of question allows you to select a label that applies to the whole record. This type is best for a text classification task.
- A TextQuestion
  - Explanation: This type of question allows you to write text. This is not suitable for a token classfication task.
- None of the above
  - Explanation: SpanQuestions let you highlight bits of text and apply a label to them. This is the best type for a token classification task.

### 6. What is the purpose of the "Save as draft" button?

**Choices**
- Submit your responses
  - Explanation: This button saves your responses, but doesn't submit them
- Save your responses without submitting them
  - Explanation: This is a valid method of loading a Hugging Face model from the Hub
- Discard a record
  - Explanation: Try again -- you cannot load a model by using the 'demos' prefix.

### 7. Argilla does not offer suggested labels automatically, you need to provide that data yourself.

**Choices**
- True
  - Explanation: You can add suggestions to your records (or update them) at any point of the project.
- False
  - Explanation: If you want to see suggested labels, you need to log them yourself when you create the records or at a later point.

### 8. Select all the necessary steps to export an Argilla dataset in full to the Hub:

**Choices**
- You need to be connected to your Argilla server: <code>client= rg.Argilla(api_url='...', api_key='...')</code>
  - Explanation: Yes, to interact with your server you'll need to instantiate it first.
- Import the dataset from the hub: <code>dataset = rg.Dataset.from_hub(repo_id='argilla/ag_news_annotated')</code>
  - Explanation: No. This is to import a dataset from the Hub into your Argilla instance.
- Load the dataset: <code>dataset = client.datasets(name='my_dataset')</code>
  - Explanation: Yes, you'll need this for further operations
- Convert the Argilla dataset into a Datasets dataset: <code>dataset = dataset.to_datasets()</code>
  - Explanation: This is not needed if you export the full dataset. Argilla will take care of this for you. However, you might need it if you're working with a subset of records.
- Use the </code>to_hub</code> method to export the dataset: <code>dataset.to_hub(repo_id='my_username/dataset_name')</code>
  - Explanation: This will push the dataset to the indicated repo id, and create a new repo if it doesn't exist.
