[pytest]
# Put the repo root on sys.path so `import backend.ingestion` works from tests/.
pythonpath = .
testpaths = tests
markers =
    integration: requires PostgreSQL with pg_textsearch
    model_integration: requires local GGUF models and llama-server
